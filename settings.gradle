pluginManagement {
    repositories {
        mavenLocal() // or your artifact repository
        gradlePluginPortal()
    }
}
rootProject.name = 'PokemonEffectiveness'
include('backend', 'frontend', 'end')
